import React from "react";
import { Title, Subtitle, Landing } from "../styles/MainStyles";

export const ComingSoon = () => {
	return (
		<Landing>
			<Subtitle>
				Looks like this page doesn't exist yet. But look forward to a useful
				feature that will be coming soon!
			</Subtitle>
		</Landing>
	);
};

export default ComingSoon;
